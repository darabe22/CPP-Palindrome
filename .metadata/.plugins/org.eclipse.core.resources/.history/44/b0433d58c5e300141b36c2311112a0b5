#pragma once

using namespace std;

template <class E>
class ArraySet{

private:
	vector<E> arraySet;

public:
	ArraySet(){arraySet = new vector<E>();};
	bool add();
	bool contains(E element);
	vector<E> toVector(return arraySet;);
};
