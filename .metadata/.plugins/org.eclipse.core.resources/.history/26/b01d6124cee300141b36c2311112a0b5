#pragma once;
#include <vector>

using namespace std;

template <class E>
class ArraySet{

private:
	vector<E> arraySet;

public:
	ArraySet(){arraySet = new vector<E>();};
	bool add(E element);
	bool contains(E element);
	vector<E> toVector(){return arraySet;};
	string toString();
};
